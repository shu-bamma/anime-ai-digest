"""
Utility functions: hashing, date parsing, text cleaning.

- content_hash(source_id, url, title) → SHA-256 string
- parse_date(date_string) → datetime (UTC)
- clean_html(html_string) → plain text
- truncate(text, max_len) → truncated string
"""
# TODO: Implement
